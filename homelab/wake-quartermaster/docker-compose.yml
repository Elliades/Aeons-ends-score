services:
  web:
    build: .
    image: wake-quartermaster:latest
    restart: unless-stopped
    network_mode: host
    environment:
      PORT: "3087"
      WOL_MAC: "18:C0:4D:A9:10:3A"
      WOL_BROADCAST: "192.168.1.255"
      WOL_HOST_NAME: "Quatermaster"
      WAKE_TOKEN: "${WAKE_TOKEN:-}"
